# TODO: Add Spring Cloud Gateway route configuration
# Routes should map:
# - /api/portfolios/** to portfolio-service
# - /api/trades/** to trade-service

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # TODO: Add route for portfolio-service
        # - id: portfolio-service
        #   uri: lb://portfolio-service
        #   predicates:
        #     - Path=/api/portfolios/**

server:
  port: 8080

